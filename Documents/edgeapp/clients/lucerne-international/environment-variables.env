# Lucerne International - Production Environment Variables
# Apply these EXACTLY in Vercel Project Settings → Environment Variables

# Supabase Configuration (CRITICAL: Use exact REACT_APP_ prefix for client vars)
REACT_APP_SUPABASE_URL=https://wvggehrxhnuvlxpaghft.supabase.co
REACT_APP_SUPABASE_ANON_KEY=[GET_FROM_SUPABASE_PROJECT_SETTINGS_API]

# Server-Only Variables (NO REACT_APP_ prefix - these are for Vercel serverless functions)
SUPABASE_URL=https://wvggehrxhnuvlxpaghft.supabase.co
SUPABASE_SERVICE_ROLE_KEY=[GET_FROM_SUPABASE_PROJECT_SETTINGS_API_SERVICE_ROLE]

# Client Configuration  
REACT_APP_TENANT_ID=lucerne
REACT_APP_CLIENT_NAME=Lucerne International
REACT_APP_ENV=production

# Optional: Admin Configuration
REACT_APP_ADMIN_EMAIL=dokonoski@lucerneintl.com

# ================================================================
# CRITICAL DEPLOYMENT NOTES:
# ================================================================

# 1. ENVIRONMENT VARIABLE NAMING:
#    - MUST use exact prefix "REACT_APP_" (NOT "NEXT_PUBLIC_")
#    - Case sensitive - use EXACT capitalization shown above

# 2. SUPABASE ANON KEY:
#    - Get from: Supabase Project → Settings → API → anon/public key
#    - This is safe to expose in frontend (read-only, RLS-protected)

# 3. VERCEL CONFIGURATION:
#    - Root Directory: LEAVE EMPTY (not "edge-app" or any subdirectory)
#    - Build Command: npm run build
#    - Node.js Version: 22.x

# 4. AUTHENTICATION SETUP:
#    - Site URL: https://lucerne-edge-app.vercel.app  
#    - Redirect URL: https://lucerne-edge-app.vercel.app/auth/callback

# 5. VERIFICATION:
#    - After applying these variables, redeploy the Vercel project
#    - Test authentication with: dokonoski@lucerneintl.com
#    - Should see in console: "✅ Found user in employees table: {role: 'admin'}"